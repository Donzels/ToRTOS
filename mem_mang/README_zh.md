本目录的内存分配器

该目录下提供两种 ToRTOS 动态内存分配实现：

1) mem0.c
   - 简单的空闲链表分配器。
   - 释放后不合并相邻空闲块。
   - 结构简单、开销低，但长期运行易产生碎片。

2) Tomem1/mem1.c
   - 字节池分配器，块按地址顺序组成环形链表。
   - 使用游标（search_ptr）分散分配位置。
   - 在分配时惰性合并相邻空闲块。
   - 使用位于末尾的哨兵块保证环的边界。

可根据内存占用、碎片容忍度和性能需求选择合适实现。
